kind: pod
apiVersion: v1 
metadata:
  name: env-test
spec:
  containers:
  - name: nginx
    image: nginx
    env: 
    - name: course
      value: 'kubernetes'
    - name: duratio
      value: '3-months'

